{
  "name": "accounting",
  "private": true,
  "scripts": {
    "setup": "pnpm --filter web run setup",
    "dev": "pnpm run -r dev",
    "prepare": "husky",
    "pre-commit": "pnpm run -r pre-commit"
  },
  "devDependencies": {
    "husky": "^9.0.11"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "19.0.2",
      "@types/react-dom": "19.0.2"
    }
  }
}
